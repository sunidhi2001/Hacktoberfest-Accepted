#include<stdio.h>
int main(){
    // One-Liner IF-Else Statement
    printf("Enter a no.:~>");
    int no_;
    scanf("%d", &no_);
    (no_ < 50) ? printf("Number < 50"):printf("Number > 50");
    return 0;
}