#include <stdio.h>
int main()
{
    int a = 0, b;
    printf("Enter The Limit:~>");
    scanf("%d", &b);
    //WHILE LOOP(First Check the condition then run the loop.)
    while /*Condition---->*/ (a < b)
    {
        printf("%d\n", a);
        a++;
    }
    return 0;
}